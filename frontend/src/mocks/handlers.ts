// import { rest } from 'msw';
// import { API_PREFIX } from 'api/api.v1';

export const handlers = [
  /**
   * Example:
   * rest.get(`${API_PREFIX}/endpoint`, (_, res, ctx) => {
   *  return res(ctx.json({ data: 'data' }));
   * }),
   */
];
